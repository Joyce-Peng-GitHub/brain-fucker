[package]
name = "brain-fucker"
version = "1.0.0"
edition = "2024"

[[bin]]
name = "bf-interpreter"
path = "src/main.rs"

[profile.release]
strip = true
lto = true

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
assert_cmd = "2.0"